/**
 * Algoritmo para guardar N personas y registrar de cada uno 
 * nombre, apellido y edad
 * Guardarlo en un arreglo de objetos e imprimirlo
 */

const registrarPersonas = () => {
    const n = 3;
    const personas = [];
    for (let i = 0; c < n; i++) {
        const nombre=prompt(`Ingrese el nombre de la persona ${i+1}`);
        const apelldo=prompt(`Ingrese el apellido de la persona ${i+1}`);
        const edad=prompt(`Ingrese la edad de la persona ${i+1}`);
        
    }
}